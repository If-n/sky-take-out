<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into dish (name, category_id, price, image, description, status, create_time, update_time, create_user, update_user)
        VALUES
            (#{name},#{categoryId},#{price},#{image},#{description},#{status},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>
    <select id="queryPage" resultType="com.sky.vo.DishVO">
        SELECT d.*,c.name as categoryName from dish as d left outer join category c on c.id=d.category_id
        <where>
            <if test="name!=null">d.name like concat('%',#{name},'%'),</if>
            <if test="status!=null">d.status=#{status},</if>
            <if test="categoryId!=null">d.category_id=#{categoryId}</if>
        </where>
        order by create_time desc
    </select>

</mapper>
